import { useState } from "react";
import blogs from "./components/blog";
import BlogPost from "./components/blogpost";

function App({ BlogPost, blogs }) {
  <div>{BlogPost}</div>;
}

export default App;
